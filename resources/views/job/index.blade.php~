@extends('app')

@section('content')

    @foreach($jobs as $job)
        <article>
            <h2>{{ $job->title }}</h2>
        </article>
    @endforeach


<a href="create">Добавить вакансию</a>
@stop
